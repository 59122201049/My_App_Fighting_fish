<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myappprojcet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="App Thai Fighting Fish"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResultActivity" />
        <activity android:name=".QuizActivity">

        </activity>
        <activity android:name=".ShowtwoActivity" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".BubbleActivity" />
        <activity android:name=".MainONEActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MouthActivity" />
        <activity android:name=".MainfourActivity" />
        <activity android:name=".maintwoActivity"></activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>